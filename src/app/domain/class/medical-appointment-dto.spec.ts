import { MedicalAppointmentDTO } from './medical-appointment-dto';

describe('MedicalAppointmentDTO', () => {
  it('should create an instance', () => {
    expect(new MedicalAppointmentDTO()).toBeTruthy();
  });
});
