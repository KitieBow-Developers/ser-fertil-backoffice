import { AppointmentinfoDTO } from './appointmentinfo-dto';

describe('AppointmentinfoDTO', () => {
  it('should create an instance', () => {
    expect(new AppointmentinfoDTO()).toBeTruthy();
  });
});
